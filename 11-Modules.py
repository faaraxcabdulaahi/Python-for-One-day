# A Module is basically a file containing a set of functions to include in your application.

#There is are core python modules, modules you can install using the pip package manager, (including Django ) as well as custom modules. 

# To import a module use the import module. 

'''A) Date time module'''

import datetime
today = datetime.date.today()
print(today)


'''B) Time module'''
import time
timestamp = time.time()
print(timestamp)