# A lambda function is a small anonynmous function.

#A lambda function can take any number of arguments, but can only have one expression. its is very similar to javascript arrow functions. 

# use lambda keyword

getSum = lambda num1, num2 : num1 + num2

print(getSum(10,4))